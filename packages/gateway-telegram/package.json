{
  "name": "@sentinel/gateway-telegram",
  "version": "0.1.0",
  "description": "Telegram channel shell: webhook endpoint, payload translation, wires gateway-core",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "test": "jest"
  },
  "dependencies": {
    "@sentinel/gateway-core": "*",
    "@sentinel/telegram-client": "*",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21"
  }
}
