PORT=3001
LOG_LEVEL=info

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o
OPENAI_VISION_MODEL=gpt-4o

REDIS_URL=redis://redis:6379
DATABASE_URL=postgres://gracesoft:gracesoft@postgres:5432/gracesoft_sentinel

# "Mother's Day Edition" (optional): personal recipe retrieval via RAG,
# queried from a Pinecone index instead of generating a recipe from a photo.
# Leave PINECONE_INDEX_NAME unset to disable this feature entirely — nothing
# else about Cook's behavior changes. When set, PINECONE_API_KEY becomes
# required too.
#
# This service only *queries* the index — it doesn't populate it. Run
# `pnpm --filter @gracesoft-sentinel/provider-recipe-pinecone run sync`
# (its own env vars: GOOGLE_SERVICE_ACCOUNT_EMAIL/PRIVATE_KEY,
# GOOGLE_DRIVE_RECIPES_FOLDER_ID, OPENAI_API_KEY, plus the three below) to
# (re)index a Google Drive folder of personal recipes into Pinecone first.
PINECONE_API_KEY=
PINECONE_INDEX_NAME=
PINECONE_NAMESPACE=

WHATSAPP_ENABLED=false
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_APP_SECRET=
WHATSAPP_WEBHOOK_VERIFY_TOKEN=

TELEGRAM_ENABLED=false
TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_SECRET=
